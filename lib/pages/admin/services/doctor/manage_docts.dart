import 'package:flutter/material.dart';

class ManageDocts extends StatelessWidget {
  const ManageDocts({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}